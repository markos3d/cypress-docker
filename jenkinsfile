#!/usr/bin/env groovy

node {
    stage('checkout') {
        checkout scm
    }

    stage('run image') {
        sh "sudo docker-compose -f hello-world-cypress/e2e/docker-compose.yml up --abort-on-container-exit cypress"
        
    } 
}
